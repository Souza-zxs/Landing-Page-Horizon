# Configure estas variáveis no dashboard da Vercel (Settings > Environment Variables).
# Nunca prefixe com VITE_ — são usadas só pelas Serverless Functions em /api, não no client.

SUPABASE_URL=https://lscitfhcmydlnuiegwos.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Senha usada para acessar o dashboard de leads em /dashboard
DASHBOARD_PASSWORD=
